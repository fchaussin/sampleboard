# Dépendances
node_modules/

# Build front
dist/
.svelte-kit/
.vite/

# Tests E2E (Playwright)
test-results/
playwright-report/
blob-report/
.playwright/

# Sortie build Docker (prod)
/artifacts/*
!/artifacts/.gitkeep

# Tauri / Rust
src-tauri/target/
# Schémas ACL régénérés à chaque build ; le projet Android (gen/android), lui, est
# committé — édité à la main (manifests) et requis par le build F-Droid (M6).
src-tauri/gen/schemas/

# Env & secrets
.env
.env.*
!.env.example

# OS / éditeurs
.DS_Store
Thumbs.db
*.log
