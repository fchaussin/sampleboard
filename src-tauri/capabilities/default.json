{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacités par défaut de Sampleboard : SQLite, système de fichiers et dialogue d'import.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    "fs:allow-appdata-write-recursive",
    "sql:default",
    "sql:allow-execute"
  ]
}
