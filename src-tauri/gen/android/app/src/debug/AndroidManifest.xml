<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
<!-- Overlay DEBUG uniquement : `tauri android dev` charge l'UI depuis le serveur Vite
     (devUrl) et a donc besoin du réseau. L'APK release, lui, reste sans aucune
     permission réseau (spec §16 — v1 100 % hors-ligne). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
